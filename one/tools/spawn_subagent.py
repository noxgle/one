from __future__ import annotations


async def spawn_subagent_tool(task: str, tasks: list[str] | None = None, model: str | None = None, tools: list[str] | None = None) -> dict:
    """Placeholder: spawn_subagent is executed by the session loop, which has
    access to the session context (providers, session manager, limits)."""
    raise RuntimeError("spawn_subagent is executed by the session loop")
