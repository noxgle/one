from __future__ import annotations


async def ask_user_tool(question: str, timeoutSec: int | None = None) -> dict:
    """Placeholder: ask_user is executed by the session loop, which has
    access to the pending-question mechanism and the event bus."""
    raise RuntimeError("ask_user is executed by the session loop")
